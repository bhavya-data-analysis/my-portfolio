---
import BaseLayout from "../layouts/BaseLayout.astro";
---

<BaseLayout>
  <section class="hero">
    <!-- your content -->
  </section>
</BaseLayout>

---
const projects = [
  {
    id: "fake-news",
    title: "Fake News Detection System",
    tag: "NLP · Machine Learning · Explainability",
    description:
      "End-to-end misinformation detection system comparing deployable cloud models with richer local ML experimentation.",

    panels: [
      {
        title: "Cloud Demo",
        caption: "TF-IDF + Logistic Regression (Deployable)",
        image: "/images/fake-news-cloud.png",
        link: "https://fake-news-detection-app-adptswkkruuf4keteyadn6.streamlit.app",
      },
      {
        title: "Local ML System",
        caption: "CNN + LR + LIME (Full ML Depth)",
        image: "/images/fake-news-local.png",
        link: "https://github.com/bhavya-data-analysis/Fake-News-Detection-app",
      },
    ],
  },

  {
    id: "sales",
    title: "Sales Analytics & Decision Intelligence System",
    tag: "Streamlit · Business Intelligence · Decision Support",
    description:
      "Single sales intelligence system delivered as two coordinated dashboards: A2 explains what is happening and why; A3 translates findings into executive decisions.",

    panels: [
      {
        title: "A2 — Diagnostic Analytics",
        caption: "Trends, drivers, profit risk",
        image: "/images/sales-a2.png",
        link: "https://xcompany-ai-sales-dashboard-bdel2x6pwedw8pcqmagvqc.streamlit.app",
      },
      {
        title: "A3 — Decision Dashboard",
        caption: "KPIs, risk flags, actions",
        image: "/images/sales-a3.png",
        link: "https://xcompany-ai-sales-dashboard-dsp5zzmrzjielnpwjfezp4.streamlit.app",
      },
    ],
  },

  {
    id: "rdi",
    title: "Reflective Decision Intelligence (RDI)",
    tag: "Decision Quality · Cognitive Risk Modeling",
    description:
      "A system for structuring free-form decisions into comparable decision-quality profiles and tracking cognitive risk over time.",

    panels: [
      {
        title: "Core System",
        caption: "Decision quality & cognitive risk",
        image: "/images/rdi.png",
        link: "https://github.com/bhavya-data-analysis/RDI",
      },
    ],
  },

  {
    id: "cv",
    title: "Computer Vision — Trash Detection (YOLOv8)",
    tag: "Computer Vision · Object Detection",
    description:
      "YOLOv8 object detection model trained on a custom dataset and evaluated using mAP metrics.",

    panels: [
      {
        title: "Detection Results",
        caption: "YOLOv8 · mAP@50–95",
        image: "/images/trash-detection-1.png",
        link: "https://github.com/bhavya-data-analysis",
      },
      {
        title: "Detection Results",
        caption: "YOLOv8 · mAP@50–95",
        image: "/images/trash-detection-2.png",
        link: "https://github.com/bhavya-data-analysis",
      },
    ],
  },

  {
    id: "jpm",
    title: "JP Morgan Chase 2014 Data Breach Analysis",
    tag: "Risk Analytics · Incident Timeline",
    description:
      "Analytical case study examining detection failures, escalation gaps, and how analytics could reduce dwell time.",

    panels: [
      {
        title: "Breach Dashboard",
        caption: "Timeline & impact analysis",
        image: "/images/jpm-breach-dashboard.png",
        link: "https://github.com/bhavya-data-analysis",
      },
    ],
  },
];
---

<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Projects · Bhavya Pandya</title>
    <link rel="stylesheet" href="/src/styles/global.css" />
  </head>

  <body>
    <section class="page-hero">
      <div class="container fade-up">
        <div class="hero-eyebrow">Applied Systems</div>
        <h1>Decision-Grade AI & Analytics</h1>
        <p class="hero-copy">
          Systems built for clarity, explainability, and operations — not demo
          sprawl. Each project holds multiple components that work together.
        </p>
      </div>
    </section>

    {projects.map((p) => (
      <section class="system-section">
        <div class="container fade-up">
          <div class="system-shell">
            <div class="system-header">
              <div class="system-meta">
                <div class="system-tag">{p.tag}</div>
                <div class="system-line"></div>
              </div>
              <div class="system-heading">
                <h2>{p.title}</h2>
                <p class="system-description">{p.description}</p>
              </div>
            </div>

            <div
              class={`panels-grid ${p.panels.length === 2 ? "two-up" : "single"}`}
            >
              {p.panels.map((panel) => (
                <div class="panel">
                  <div class="panel-top">
                    <div class="panel-label">Component</div>
                    <div class="panel-title">{panel.title}</div>
                    <p class="panel-caption">{panel.caption}</p>
                  </div>

                  <div class="panel-media">
                    <img src={panel.image} alt={panel.title} loading="lazy" />
                  </div>

                  <div class="panel-actions">
                    <a
                      class="text-link"
                      href={panel.link}
                      target="_blank"
                      rel="noreferrer"
                    >
                      Open
                    </a>
                  </div>
                </div>
              ))}
            </div>
          </div>
        </div>
      </section>
    ))}

    <style>
      .page-hero {
        padding: 80px 0 30px;
      }

      .hero-eyebrow {
        text-transform: uppercase;
        letter-spacing: 0.18em;
        font-size: 0.8rem;
        color: var(--accent);
        margin-bottom: 12px;
      }

      .hero-copy {
        margin-top: 18px;
        max-width: 640px;
      }

      .system-section {
        padding: 46px 0 30px;
      }

      .system-shell {
        background: linear-gradient(140deg, rgba(255, 255, 255, 0.02), rgba(255, 255, 255, 0.01));
        border: 1px solid var(--glass-border);
        border-radius: 20px;
        padding: 32px;
        box-shadow: 0 24px 70px rgba(0, 0, 0, 0.45);
        display: grid;
        gap: 28px;
      }

      .system-header {
        display: grid;
        grid-template-columns: auto 1fr;
        gap: 22px;
        align-items: start;
      }

      .system-tag {
        color: var(--accent);
        letter-spacing: 0.14em;
        font-size: 0.78rem;
        text-transform: uppercase;
        white-space: nowrap;
      }

      .system-line {
        width: 48px;
        height: 1px;
        background: var(--border);
        margin-top: 12px;
      }

      .system-heading h2 {
        font-size: clamp(2.1rem, 4vw, 2.9rem);
        margin-bottom: 10px;
      }

      .system-description {
        color: var(--muted);
        max-width: 900px;
      }

      .panels-grid {
        display: grid;
        gap: 18px;
        width: 100%;
      }

      .panels-grid.two-up {
        grid-template-columns: repeat(2, minmax(0, 1fr));
      }

      .panels-grid.single {
        grid-template-columns: minmax(0, 0.9fr);
        max-width: 780px;
      }

      .panel {
        background: var(--bg-soft);
        border: 1px solid var(--border);
        border-radius: 16px;
        padding: 18px;
        display: grid;
        gap: 12px;
        position: relative;
        overflow: hidden;
        box-shadow: inset 0 1px 0 rgba(255, 255, 255, 0.04);
      }

      .panel::after {
        content: "";
        position: absolute;
        inset: 0;
        background: radial-gradient(circle at 20% 20%, rgba(255, 122, 69, 0.08), transparent 40%);
        pointer-events: none;
      }

      .panel-top {
        position: relative;
        z-index: 1;
        display: grid;
        gap: 4px;
      }

      .panel-label {
        text-transform: uppercase;
        letter-spacing: 0.12em;
        font-size: 0.7rem;
        color: var(--muted);
      }

      .panel-title {
        font-size: 1.1rem;
        font-weight: 600;
      }

      .panel-caption {
        color: var(--muted);
      }

      .panel-media {
        position: relative;
        z-index: 1;
        border-radius: 12px;
        border: 1px solid var(--border);
        overflow: hidden;
        background: var(--card);
        aspect-ratio: 16 / 9;
        max-height: 45vh;
        min-height: 220px;
        box-shadow: 0 20px 60px rgba(0, 0, 0, 0.35);
      }

      .panel-media img {
        width: 100%;
        height: 100%;
        object-fit: cover;
        display: block;
      }

      .panel-actions {
        position: relative;
        z-index: 1;
        display: flex;
        justify-content: flex-end;
        align-items: center;
      }

      .text-link {
        color: var(--text);
        font-weight: 500;
        display: inline-flex;
        align-items: center;
        gap: 8px;
        padding: 8px 12px;
        border-radius: 999px;
        border: 1px solid var(--border);
        background: rgba(255, 255, 255, 0.02);
        transition: all 0.2s var(--ease-premium);
      }

      .text-link:hover {
        border-color: rgba(255, 122, 69, 0.4);
        color: var(--accent);
        transform: translateY(-2px);
      }

      @media (max-width: 960px) {
        .system-header {
          grid-template-columns: 1fr;
        }

        .panels-grid.two-up {
          grid-template-columns: 1fr;
        }
      }
    </style>
  </body>
</html>
